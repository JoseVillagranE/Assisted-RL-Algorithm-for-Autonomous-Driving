run_type: 'Test'
run_id: 'Test'
seed: 1

train:
  checkpoint_every: 0
  episodes: 10
  steps: 200

agent:
  sensor:
    dashboard_camera: False


exo_agents:
  pedestrian:
    spawn: False

  vehicle:
    spawn: False

model:
  type: "DummyModel"
  id: '0'
